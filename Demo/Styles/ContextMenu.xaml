<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Linear-styled MenuItem.
         IMPORTANT: PART_Popup must be a direct child of Border (not nested
         inside a Grid), because WPF MenuItem's internal logic looks for it
         at the template root and may have layout issues otherwise.
         When a user hovers a parent MenuItem, IsSubmenuOpen becomes true
         and PART_Popup shows the submenu — without it, NullReferenceException
         is thrown when the submenu tries to render. -->
    <Style x:Key="LinearMenuItem" TargetType="MenuItem">
        <Setter Property="FontFamily" Value="{StaticResource FontPrimary}"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="500"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding" Value="10,6"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Border x:Name="Border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{StaticResource RadiusStd}"
                            Padding="{TemplateBinding Padding}">
                        <Grid>
                            <!-- Main row: icon / header / submenu arrow -->
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="MenuItemIconColumnGroup"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="MenuItemIGTColumnGroup"/>
                            </Grid.ColumnDefinitions>

                            <ContentPresenter Grid.Column="0"
                                              x:Name="Icon"
                                              ContentSource="Icon"
                                              Margin="0,0,8,0"
                                              VerticalAlignment="Center"
                                              Width="16" Height="16"/>

                            <ContentPresenter Grid.Column="1"
                                              x:Name="HeaderHost"
                                              ContentSource="Header"
                                              RecognizesAccessKey="True"
                                              VerticalAlignment="Center"
                                              Margin="0,0,16,0"
                                              HorizontalAlignment="Left"/>

                            <TextBlock Grid.Column="2"
                                       x:Name="IGTSubmenuIndicator"
                                       Text="›"
                                       FontSize="14"
                                       Foreground="{StaticResource TextQuat}"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Right"
                                       Margin="6,0,0,0"
                                       Visibility="Collapsed"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <!-- Show submenu arrow for items that have sub-items -->
                        <Trigger Property="HasItems" Value="True">
                            <Setter TargetName="IGTSubmenuIndicator" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource BtnBgHover}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.4"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
<!-- Submenu template — used when Role=SubmenuHeader. This is what
             WPF uses to render the parent MenuItem AND its child Popup when
             hovering. Without this, hovering a parent item crashes. -->
        <Style.Resources>
            <ControlTemplate x:Key="{x:Static MenuItem.SubmenuHeaderTemplateKey}"
                             TargetType="MenuItem">
                <Border x:Name="Border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{StaticResource RadiusStd}"
                        Padding="{TemplateBinding Padding}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" SharedSizeGroup="MenuItemIconColumnGroup"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto" SharedSizeGroup="MenuItemIGTColumnGroup"/>
                        </Grid.ColumnDefinitions>

                        <ContentPresenter Grid.Column="0"
                                          ContentSource="Icon"
                                          Margin="0,0,8,0"
                                          VerticalAlignment="Center"
                                          Width="16" Height="16"/>

                        <ContentPresenter Grid.Column="1"
                                          ContentSource="Header"
                                          RecognizesAccessKey="True"
                                          VerticalAlignment="Center"
                                          Margin="0,0,16,0"
                                          HorizontalAlignment="Left"/>

                        <TextBlock Grid.Column="2"
                                   Text="›"
                                   FontSize="14"
                                   Foreground="{StaticResource TextQuat}"
                                   VerticalAlignment="Center"
                                   HorizontalAlignment="Right"
                                   Margin="6,0,0,0"/>

                        <!-- PART_Popup inside the Grid (Popup is allowed
                             alongside other Grid children; WPF handles layout
                             correctly because Popup is special. WPF walks the
                             visual tree to find PART_Popup — placement inside
                             Grid still satisfies the lookup.) -->
                        <Popup x:Name="PART_Popup"
                               Grid.Column="0" Grid.ColumnSpan="3"
                               AllowsTransparency="True"
                               Placement="Right"
                               HorizontalOffset="-2"
                               VerticalOffset="-4"
                               IsOpen="{TemplateBinding IsSubmenuOpen}"
                               Focusable="False"
                               PopupAnimation="Fade">
                            <Border Background="{StaticResource SurfaceElevated}"
                                    BorderBrush="{StaticResource BorderStandard}"
                                    BorderThickness="1"
                                    CornerRadius="{StaticResource RadiusStd}"
                                    Padding="6">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="16" ShadowDepth="2" Opacity="0.4" Color="Black"/>
                                </Border.Effect>
                                <ScrollViewer VerticalScrollBarVisibility="Auto">
                                    <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle"
                                                    Grid.IsSharedSizeScope="True"/>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                </Border>
                <ControlTemplate.Triggers>
                    <Trigger Property="IsHighlighted" Value="True">
                        <Setter TargetName="Border" Property="Background" Value="{StaticResource BtnBgHover}"/>
                    </Trigger>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="Opacity" Value="0.4"/>
                    </Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate>
        </Style.Resources>
    </Style>

    <!-- Linear-styled Separator -->
    <Style x:Key="LinearSeparator" TargetType="Separator">
        <Setter Property="Background" Value="{StaticResource BorderSubtle}"/>
        <Setter Property="Height" Value="1"/>
        <Setter Property="Margin" Value="6,4"/>
    </Style>

    <!-- Linear-styled ContextMenu container.
         ItemContainerStyle is NOT set — the implicit Style below applies
         LinearMenuItem automatically. Double-application of the style causes
         NullReferenceException when submenus open. -->
    <Style x:Key="LinearContextMenu" TargetType="ContextMenu">
        <Setter Property="Background" Value="{StaticResource SurfaceElevated}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderStandard}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="6"/>
        <Setter Property="HasDropShadow" Value="False"/>
        <Setter Property="Effect" Value="{StaticResource ShadowDialog}"/>
        <Style.Resources>
            <Style TargetType="ScrollViewer" BasedOn="{StaticResource LinearScrollViewer}"/>
        </Style.Resources>
    </Style>

    <!-- Apply globally -->
    <Style TargetType="ContextMenu" BasedOn="{StaticResource LinearContextMenu}"/>
    <Style TargetType="MenuItem" BasedOn="{StaticResource LinearMenuItem}"/>
    <Style TargetType="Separator" BasedOn="{StaticResource LinearSeparator}"/>

</ResourceDictionary>