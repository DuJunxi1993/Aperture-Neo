<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Linear GridSplitter: 8px transparent hit zone + 1px semi-transparent white line, brighter on hover -->
    <Style TargetType="GridSplitter">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="GridSplitter">
                    <Grid Background="Transparent" SnapsToDevicePixels="True" Width="8">
                        <Rectangle x:Name="Line" Width="1"
                                   Fill="{StaticResource BorderSubtle}"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Stretch"
                                   SnapsToDevicePixels="True"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Line" Property="Width" Value="2"/>
                            <Setter TargetName="Line" Property="Fill" Value="{StaticResource BorderStandard}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
