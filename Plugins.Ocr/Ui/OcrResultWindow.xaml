<Window x:Class="ApertureNeo.Plugins.Ocr.Ui.OcrResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OCR 文字提取"
        Width="640" Height="480"
        MinWidth="360" MinHeight="280"
        WindowStartupLocation="CenterOwner"
        Background="#FFF8F8F8"
        Foreground="#FF1F1F1F"
        FontFamily="Microsoft YaHei UI, Segoe UI"
        FontSize="12"
        ShowInTaskbar="False"
        SizeToContent="Manual"
        WindowStyle="SingleBorderWindow">

    <Window.Resources>
        <Style x:Key="FlatButton" TargetType="Button">
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#FFD0D0D0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource FlatButton}">
            <Setter Property="Background" Value="#FF1F6FEB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#FF1F6FEB"/>
        </Style>
    </Window.Resources>

    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBlock Text="图片: " Foreground="#FF777777"/>
            <TextBlock x:Name="FileNameText" Text="—" FontWeight="SemiBold"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,4,0,10">
            <TextBlock Text="状态: " Foreground="#FF777777"/>
            <TextBlock x:Name="StatusText" Text="就绪" Foreground="#FF1F6FEB"/>
            <TextBlock x:Name="MetaText" Margin="12,0,0,0" Foreground="#FF999999"/>
        </StackPanel>

        <Border Grid.Row="2" BorderBrush="#FFD0D0D0" BorderThickness="1" Background="White">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="0">
                <TextBox x:Name="ResultBox"
                         IsReadOnly="True"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         BorderThickness="0"
                         Background="Transparent"
                         Padding="10"
                         FontFamily="Microsoft YaHei UI, Segoe UI"
                         FontSize="13"
                         VerticalScrollBarVisibility="Disabled"/>
            </ScrollViewer>
        </Border>

        <TextBlock Grid.Row="3" x:Name="ErrorText" Foreground="#FFD93025" Margin="0,8,0,0"
                   TextWrapping="Wrap" Visibility="Collapsed"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button x:Name="BtnCopyAll" Style="{StaticResource PrimaryButton}" Content="复制全文" Click="BtnCopyAll_Click" Margin="0,0,8,0"/>
            <Button x:Name="BtnCopyLines" Style="{StaticResource FlatButton}" Content="复制每行" Click="BtnCopyLines_Click" Margin="0,0,8,0"/>
            <Button Style="{StaticResource FlatButton}" Content="关闭" Click="BtnClose_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
