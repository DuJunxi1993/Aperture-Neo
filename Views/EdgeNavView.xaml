<UserControl x:Class="ApertureNeo.Views.EdgeNavView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="48" d:DesignWidth="48">
    <Border x:Name="EdgeNavBorder"
            IsHitTestVisible="True">
        <Button x:Name="BtnEdgeNav" Style="{StaticResource LinearEdgeButton}"
                Command="{Binding NavigateCommand}">
            <ui:SymbolIcon x:Name="EdgeIcon" FontSize="18"/>
        </Button>
    </Border>
</UserControl>