<UserControl x:Class="ApertureNeo.Views.ExitFullscreenHintView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="28" d:DesignWidth="200">
    <Border x:Name="ExitFullscreenHint"
            Background="{StaticResource SurfaceFloating}"
            BorderBrush="{StaticResource BorderStandard}" BorderThickness="1"
            CornerRadius="{StaticResource RadiusPanel}" Padding="10,5"
            IsHitTestVisible="True"
            RenderTransformOrigin="0.5,0.5">
        <Border.RenderTransform>
            <TranslateTransform x:Name="ExitFullscreenTransform" Y="-50"/>
        </Border.RenderTransform>
        <StackPanel Orientation="Horizontal">
            <ui:SymbolIcon Symbol="ArrowMinimize20" FontSize="14"
                           Foreground="{StaticResource TextSecondary}"
                           VerticalAlignment="Center"/>
            <TextBlock Text="{DynamicResource Fullscreen_ExitHint}"
                       FontSize="11" FontWeight="600"
                       Foreground="{StaticResource TextSecondary}"
                       VerticalAlignment="Center" Margin="6,0,0,0"/>
        </StackPanel>
    </Border>
</UserControl>