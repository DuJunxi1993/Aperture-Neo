<Window x:Class="ApertureNeo.Views.FirstRunDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="欢迎使用 Aperture Neo"
        Width="440" Height="280"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        TextOptions.TextFormattingMode="Ideal"
        UseLayoutRounding="True"
        FontFamily="{DynamicResource FontPrimary}"
        Foreground="{DynamicResource TextPrimary}">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/DesignTokens.xaml"/>
                <ResourceDictionary Source="/Styles/Annotation.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Window.Template>
        <ControlTemplate TargetType="{x:Type Window}">
            <Grid Margin="12">
                <Border CornerRadius="8"
                        Background="{StaticResource SurfaceCanvas}"
                        BorderBrush="{StaticResource BorderStrong}"
                        BorderThickness="1"
                        SnapsToDevicePixels="True">
                    <Border.Effect>
                        <StaticResource ResourceKey="ShadowDialog"/>
                    </Border.Effect>
                    <ContentPresenter/>
                </Border>
            </Grid>
        </ControlTemplate>
    </Window.Template>

    <Grid Margin="20,18,20,18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="欢迎使用 Aperture Neo"
                   FontSize="18" FontWeight="700"
                   Foreground="{StaticResource TextPrimary}"
                   Margin="0,0,0,8"/>

        <TextBlock Grid.Row="1"
                   TextWrapping="Wrap"
                   FontSize="13"
                   Foreground="{StaticResource TextSecondary}"
                   Margin="0,0,0,14">
            <Run Text="为了让截图和 OCR 随时可用,推荐让 Aperture Neo 在后台保持运行:"/>
        </TextBlock>

        <StackPanel Grid.Row="2" Margin="0,0,0,14">
            <CheckBox x:Name="AutoStartCheck"
                      Content="开机时自动启动"
                      IsChecked="True"
                      FontSize="13"
                      Margin="0,0,0,8"
                      Foreground="{StaticResource TextPrimary}"/>
            <CheckBox x:Name="CloseToTrayCheck"
                      Content="关闭主窗口时最小化到托盘(而非退出)"
                      IsChecked="True"
                      FontSize="13"
                      Foreground="{StaticResource TextPrimary}"/>
            <TextBlock Text="(关闭后可通过系统托盘图标或全局快捷键 Ctrl+Alt+A 重新唤起)"
                       FontSize="11"
                       Foreground="{StaticResource TextTertiary}"
                       TextWrapping="Wrap"
                       Margin="22,6,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button x:Name="LaterBtn"
                    Content="稍后"
                    Click="LaterBtn_Click"
                    MinWidth="80" Margin="0,0,8,0"
                    Style="{StaticResource LinearToolButton}"/>
            <Button x:Name="OkBtn"
                    Content="好的"
                    Click="OkBtn_Click"
                    IsDefault="True"
                    MinWidth="80"
                    Style="{StaticResource LinearPrimaryButton}"/>
        </StackPanel>
    </Grid>
</Window>